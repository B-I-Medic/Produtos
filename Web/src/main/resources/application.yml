spring:
  application:
    name: Produtos
  r2dbc:
    url: ${PRODUTO_SPRING_R2DBC_URL}
    username: ${PRODUTO_SPRING_R2DBC_USERNAME}
    password: ${PRODUTO_SPRING_R2DBC_PASSWORD}
  flyway:
    url: ${PRODUTO_FLYWAY_DATASOURCE_URL}
    user: ${PRODUTO_FLYWAY_DATASOURCE_USER}
    password: ${PRODUTO_FLYWAY_DATASOURCE_PASSWORD}
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${SMTP_USERNAME}
    password: ${SMTP_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  forward-headers-strategy: framework

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /api-docs
