spring:
  application:
    name: produto-web-dev
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/produto
    username: app-web-dev
    password: webapi123
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${SMTP_USERNAME}
    password: ${SMTP_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

springdoc:
  swagger-ui:
    doc-expansion: none